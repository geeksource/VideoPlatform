@{
    ViewData["Title"] = "Welcome to VideoPlatformPortal";
}

<div class="text-center">
    <h1 class="display-4">Welcome to VideoPlatformPortal</h1>
    <p>Your personal platform for videos and content.</p>
    <p>
        <a class="btn btn-primary" href="@Url.Action("Register", "Account")">Sign Up</a>
        <a class="btn btn-secondary" href="@Url.Action("Login", "Account")">Login</a>
    </p>
</div>
